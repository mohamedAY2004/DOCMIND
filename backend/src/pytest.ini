[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    integration: requires PostgreSQL (including disposable migration databases)
    pure: runs without PostgreSQL or external services
filterwarnings =
    ignore::DeprecationWarning
